# Root .env.example — copy to .env if you want to override defaults.
# The docker-compose.yml already wires sensible defaults; you usually
# don't need to override anything for local demos.

GATEWAY_PORT=80
TRAEFIK_DASHBOARD_PORT=8080

# JWT signing key — picked up by auth-service via docker-compose interpolation.
# Override locally by creating a .env in this directory with a real secret:
#   python3 -c 'import secrets; print("JWT_SECRET_KEY=" + secrets.token_urlsafe(48))' >> .env
JWT_SECRET_KEY=change-me-in-production-please

# Inject chaos in payment service: 0.0 = always succeed, 0.5 = 50% declines
PAYMENT_FAIL_RATE=0.0
